/// Desktop platform stub for flutter_neo_shield.
///
/// This class satisfies Flutter's plugin registration for Linux,
/// which doesn't yet have a native implementation.
/// macOS and Windows have native plugins — no stubs needed.
library;

/// Linux plugin stub.
class FlutterNeoShieldLinux {
  /// Registers the Linux plugin (no-op — native implementation pending).
  static void registerWith() {}
}
