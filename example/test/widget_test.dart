import 'package:flutter_test/flutter_test.dart';

void main() {
  test('example app placeholder', () {
    // Example app tests are run via integration tests, not widget tests.
    expect(true, isTrue);
  });
}
